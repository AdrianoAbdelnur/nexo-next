import DashboardSessionLayout from '@/src/components/dashboard-session-layout';

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <DashboardSessionLayout>{children}</DashboardSessionLayout>;
}
